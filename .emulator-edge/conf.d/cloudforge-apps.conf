# Generated by DefaultEmulatorEdgeRuntime.reconcile

server {
    listen 80 default_server;
    server_name _;
    access_log /var/log/nginx/access.log combined;
    default_type text/plain;
    location = /favicon.ico { access_log off; return 204; }
    location / { return 404 "CloudForge application route not found\n"; }
}

server {
    listen 80;
    server_name nginx.cloudforge.localhost;
    charset utf-8;

    location = /favicon.ico { access_log off; return 204; }

    location / {
        default_type text/plain;
        return 200 "CloudForge emulator edge (nginx)\n\nUse Host-based URLs (http:// - bare names may open a search):\n  http://localstack.cloudforge.localhost/\n  http://ministack.cloudforge.localhost/\n  http://stackport.cloudforge.localhost/\n  http://manager.cloudforge.localhost/\n  http://nginx.cloudforge.localhost/\n\nSet up hosts: ./scripts/setup-cloudforge-local-hosts.sh\nReconcile: mvn -f cfc-testing cloudforge:emulator-edge-reconcile\n\nActive proxy routes:\n  http://emulator.cloudforge.localhost/ -> :4566\n  http://ministack.cloudforge.localhost/ -> :4566\n  http://stackport.cloudforge.localhost/ -> :8888\n";
    }
}

server {
    listen 80;
    server_name emulator.cloudforge.localhost;

    location = /favicon.ico {
        access_log off;
        return 204;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host localhost;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_pass http://host.docker.internal:4566;
    }
}

server {
    listen 80;
    server_name ministack.cloudforge.localhost;

    location = /favicon.ico {
        access_log off;
        return 204;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host localhost;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_pass http://host.docker.internal:4566;
    }
}

server {
    listen 80;
    server_name stackport.cloudforge.localhost;

    location = /favicon.ico {
        access_log off;
        return 204;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_pass http://host.docker.internal:8888;
    }
}

